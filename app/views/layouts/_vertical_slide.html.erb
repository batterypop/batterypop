<a href="<%= slide_url %>"  <% if !(local_assigns[:slide_class].nil?) %>class="<%= slide_class %>"<% end %>>
	<div class="thumb"><%= slide_image %></div>
	<div class="summary">
		<h4><%= slide_title %></h4>
		<p><small><%= slide_text %></small></p>
	</div>
</a>