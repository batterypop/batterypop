<a href="<%= slide_url %>">
	<div class="thumb"><%= slide_image %></div>
	<div class="summary">
		<h4><%= slide_title %></h4>
		<% if !(local_assigns[:slide_text].nil?) %><p><%= slide_text %></p><% end %>
	</div>
</a>