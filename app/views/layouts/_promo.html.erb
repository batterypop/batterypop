<!-- start promo --	>
<div class="row">
	<!-- large image right slideshow -->
	<div class="col-lg-9 col-push-3">
		
		<ul class="bxslider home-featured">
			<% @features.each do |feature| %>
				<li><%= feature.content.html_safe %></li>
			<% end %>

		</ul>
	</div>
	<!-- end large image right slideshow -->
	

	
		
	<div class="col-lg-3 col-pull-9">
		<div class="col-lg-12">
			<span id="home-carousel-prev"></span> | <span id="home-carousel-next"></span>
		</div>
		<ul class="bxslider home-side-carousel">
			<% @shows.each do |show| %>
				<li>
					

					<%= render partial: "layouts/horizontal_slide", :locals => {
								:slide_title => show.title, 
							 	:slide_text => show.subtitle, 
								:slide_image => image_tag(show.image, :alt => show.title, :class => "img-responsive thumbnail media-object pull-left show-episode-image", :width => '80', :height => '80'), 
								:slide_url => show_path(Show.friendly.find(show.id))} %>

				</li>
			<% end %>
		</ul>
		
		
	</div>
	<!-- end show promo -->
</div>