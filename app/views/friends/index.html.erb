<%= render partial: "layouts/show_friend_listing", 
	:locals => {
		:listing_title => @title, :listing_description =>  @description,  :list_items => @friends, :list_class => "thumb-logo", :navigation => 'paginate'

} %>